import moment from 'moment'
import { DEFAULT_FORMAT } from '../../constants/momentFormats'

export const START_DATE = moment('04.09.476', DEFAULT_FORMAT)
export const END_DATE = moment('01.01.2000', DEFAULT_FORMAT)
export const DEFAULT_START_DATE = moment('29.05.1453', DEFAULT_FORMAT)

export const MAX_SPEED = 5
export const DEFAULT_SPEED = 2

export const DEFAULT_STEP_TIMEOUT = 300

export const DEFAULT_REQUEST_PERIOD = 15000
